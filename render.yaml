services:
  - type: web
    name: vera-message-engine
    env: python
    buildCommand: "pip install -r requirements.txt && python seed_server.py"
    startCommand: "uvicorn server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
